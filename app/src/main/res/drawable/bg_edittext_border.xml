<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Màu nền -->
    <solid android:color="@color/bgcolor_txtedit_login" />
    <!-- Border -->
    <stroke android:width="1dp" android:color="@color/bordercolor_txtedit_login" />

    <!-- Border Radius -->
    <corners android:radius="4dp" />
    <!-- Padding bên trong -->
    <padding
        android:bottom="10dp"
        android:left="10dp"
        android:right="10dp"
        android:top="10dp" />
</shape>
